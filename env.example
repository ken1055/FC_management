# Vercel環境変数設定例
# これらの値をVercelダッシュボードの Environment Variables で設定してください

# ===== 必須設定 =====

# セッション暗号化キー（本番環境では強力なランダム文字列に変更）
# 32文字以上の英数字・記号を組み合わせた文字列を設定してください
SESSION_SECRET=your-very-strong-secret-key-here-min-32-characters-with-special-chars

# Node.js環境設定
NODE_ENV=production

# ===== セキュリティ設定 =====

# HTTPS無効化（本番では通常設定不要）
# DISABLE_HTTPS=true

# ===== メール設定（オプション） =====

# SMTP設定
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# 管理者メールアドレス（複数設定可能、カンマ区切り）
ADMIN_EMAILS=admin1@company.com,admin2@company.com

# ===== 管理者昇格パスワード（オプション） =====

# 代理店ユーザーが管理者に昇格する際のパスワード
ADMIN_PROMOTION_PASS=your-admin-promotion-password

# ===== データベース設定 =====

# Supabase設定（推奨）
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# PostgreSQL接続URL（直接接続の場合）
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Redis設定（セッションストア用）
# REDIS_URL=redis://username:password@hostname:port

# ===== 設定例・注意事項 =====

# 1. SESSION_SECRET の生成例:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. 本番環境での必須設定:
#    - SESSION_SECRET: 必須
#    - NODE_ENV: production に設定
#
# 3. セキュリティ強化（推奨）:
#    - 初期管理者パスワード（admin/admin）の変更
#    - ADMIN_PROMOTION_PASS の設定
#    - 定期的な SESSION_SECRET の更新
#
# 4. データベース移行（本格運用時）:
#    - Vercel Postgres または他のDBサービスへの移行
#    - セッションストアをRedisに変更 