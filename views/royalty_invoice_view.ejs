<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"
    />
    <style>
      body {
        background: #f8f9fa;
      }
      .viewer {
        height: 85vh;
        border: 1px solid #dee2e6;
        border-radius: 0.5rem;
        background: #fff;
      }
    </style>
  </head>
  <body class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 mb-0">
        <i class="bi bi-file-pdf text-danger"></i>
        請求書プレビュー - <%= calculation.store_name %>（<%=
        calculation.calculation_year %>/<%=
        String(calculation.calculation_month).padStart(2,'0') %>）
      </h1>
      <div class="btn-group">
        <a
          href="/royalty/invoice/<%= calculation.id %>"
          class="btn btn-primary"
        >
          <i class="bi bi-download"></i> ダウンロード
        </a>
        <button class="btn btn-outline-secondary" onclick="window.print()">
          <i class="bi bi-printer"></i> 印刷
        </button>
        <a
          href="/royalty/calculations?year=<%= calculation.calculation_year %>&month=<%= calculation.calculation_month %>"
          class="btn btn-outline-dark"
        >
          <i class="bi bi-arrow-left"></i> 戻る
        </a>
      </div>
    </div>

    <div class="viewer">
      <iframe
        src="/royalty/invoice/<%= calculation.id %>/pdf?v=<%= Date.now() %>"
        title="invoice"
        width="100%"
        height="100%"
        style="border: 0"
      ></iframe>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
