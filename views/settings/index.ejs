<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
    />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
      <div class="container">
        <a class="navbar-brand" href="/">
          <i class="bi bi-gear"></i> システム設定
        </a>
        <div class="navbar-nav ms-auto">
          <% if (session.user && session.user.role === 'admin') { %>
          <a class="nav-link" href="/admin">
            <i class="bi bi-speedometer2"></i> 管理者ダッシュボード
          </a>
          <% } %>
          <a class="nav-link" href="/auth/logout">
            <i class="bi bi-box-arrow-right"></i> ログアウト
          </a>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <div class="row">
        <div class="col-md-8 mx-auto">
          <div class="card">
            <div class="card-header">
              <h4 class="mb-0"><i class="bi bi-gear"></i> システム設定</h4>
            </div>
            <div class="card-body">
              <% if (typeof query !== 'undefined' && query.success) { %>
              <div class="alert alert-success alert-dismissible fade show">
                <i class="bi bi-check-circle"></i> 設定を保存しました。
                <button
                  type="button"
                  class="btn-close"
                  data-bs-dismiss="alert"
                ></button>
              </div>
              <% } %>

              <!-- 公式ラインURL設定 -->
              <div class="mb-4">
                <h5><i class="bi bi-chat-dots"></i> 公式LINE設定</h5>
                <p class="text-muted">
                  代理店ユーザーに表示される公式LINEのURLを設定します。
                </p>

                <form method="post" action="/settings/official-line">
                  <div class="mb-3">
                    <label for="url" class="form-label">公式LINE URL</label>
                    <input
                      type="url"
                      id="url"
                      name="url"
                      class="form-control"
                      value="<%= officialLineUrl || '' %>"
                      placeholder="https://line.me/R/ti/p/..."
                    />
                    <div class="form-text">
                      LINEの公式アカウントのURLを入力してください。空にすると代理店画面でリンクが非表示になります。
                    </div>
                  </div>

                  <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                      <i class="bi bi-save"></i> 保存
                    </button>
                    <a href="/admin" class="btn btn-secondary">
                      <i class="bi bi-arrow-left"></i>
                      管理者ダッシュボードに戻る
                    </a>
                  </div>
                </form>
              </div>

              <!-- プレビュー -->
              <% if (officialLineUrl) { %>
              <div class="mt-4 p-3 bg-light rounded">
                <h6>代理店画面での表示プレビュー:</h6>
                <a
                  href="<%= officialLineUrl %>"
                  target="_blank"
                  class="btn btn-success"
                >
                  <i class="bi bi-chat-dots"></i> 公式LINEはこちら
                </a>
              </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
