<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>売上管理 - 代理店管理システム</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"
    />
  </head>
  <body class="container py-5">
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4 rounded">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">代理店管理システム</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if (typeof session !== 'undefined' && session && session.user &&
            session.user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/agencies/list">代理店一覧</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/sales/list">売上管理</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/groups/list">グループ管理</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/api/users/list">アカウント管理</a>
            </li>
            <% } %> <% if (typeof session !== 'undefined' && session &&
            session.user && session.user.role === 'agency') { %>
            <li class="nav-item">
              <a class="nav-link text-warning" href="/auth/promote"
                >管理者・役員に切り替え</a
              >
            </li>
            <% if (session.user.agency_id) { %>
            <li class="nav-item">
              <a
                class="nav-link"
                href="/agencies/profile/<%= session.user.agency_id %>"
                >マイプロフィール</a
              >
            </li>
            <% } %>
            <li class="nav-item">
              <a class="nav-link" href="/sales/list">売上管理</a>
            </li>
            <% if (session.user.agency_id) { %>
            <li class="nav-item">
              <a
                class="nav-link"
                href="/materials/<%= session.user.agency_id %>"
                >商品資料格納庫</a
              >
            </li>
            <% } %> <% } %> <% if (typeof session !== 'undefined' && session &&
            session.user && (session.user.role === 'admin')) { %> <% } %> <% if
            (typeof session !== 'undefined' && session && session.user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/auth/logout">ログアウト</a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/auth/login">ログイン</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="card shadow-sm p-4 mb-4">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">売上管理 - 代理店選択</h2>
        <a href="/" class="btn btn-secondary">ダッシュボードへ戻る</a>
      </div>

      <div class="mb-3">
        <p class="text-muted">
          代理店を選択して個別の売上データを確認できます。新しい売上データを登録する場合は、代理店を選択後に登録してください。
        </p>
      </div>

      <!-- 代理店一覧カード -->
      <% if (agencies.length === 0) { %>
      <div class="text-center py-5">
        <i class="bi bi-building fs-1 text-muted"></i>
        <h5 class="mt-3 text-muted">代理店がありません</h5>
        <p class="text-muted">まず代理店を登録してください。</p>
        <a href="/agencies/new" class="btn btn-primary">代理店を新規登録</a>
      </div>
      <% } else { %>

      <div class="row g-3">
        <% agencies.forEach(agency => { %>
        <div class="col-md-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div
                class="d-flex justify-content-between align-items-start mb-3"
              >
                <h5 class="card-title mb-0">
                  <i class="bi bi-building text-primary"></i>
                  <%= agency.name %>
                </h5>
                <% if (agency.sales_count > 0) { %>
                <span class="badge bg-success"
                  ><%= agency.sales_count %>件</span
                >
                <% } else { %>
                <span class="badge bg-secondary">0件</span>
                <% } %>
              </div>

              <div class="mb-3">
                <div class="row text-center">
                  <div class="col-6">
                    <div class="border-end">
                      <div class="fs-6 text-muted">登録済み売上</div>
                      <div class="fs-5 fw-bold text-success">
                        <%= agency.sales_count || 0 %>件
                      </div>
                    </div>
                  </div>
                  <div class="col-6">
                    <div class="fs-6 text-muted">合計売上</div>
                    <div class="fs-5 fw-bold text-primary">
                      <% if (agency.total_sales) { %> ¥<%=
                      parseInt(agency.total_sales).toLocaleString() %> <% } else
                      { %> ¥0 <% } %>
                    </div>
                  </div>
                </div>
              </div>

              <div class="d-grid gap-2">
                <a
                  href="/sales/agency/<%= agency.id %>"
                  class="btn btn-primary"
                >
                  <i class="bi bi-graph-up"></i> 売上詳細を見る
                </a>
                <div class="btn-group" role="group">
                  <a
                    href="/sales/new?agency_id=<%= agency.id %>"
                    class="btn btn-outline-success btn-sm"
                  >
                    <i class="bi bi-plus-circle"></i> 売上登録
                  </a>
                  <a
                    href="/agencies/profile/<%= agency.id %>"
                    class="btn btn-outline-info btn-sm"
                  >
                    <i class="bi bi-person"></i> プロフィール
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>

      <!-- 統計情報 -->
      <div class="mt-4">
        <div class="row">
          <div class="col-md-4">
            <div class="card bg-light">
              <div class="card-body text-center">
                <h6 class="card-title">代理店数</h6>
                <div class="fs-4 fw-bold text-primary">
                  <%= agencies.length %>
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="card bg-light">
              <div class="card-body text-center">
                <h6 class="card-title">売上登録済み代理店</h6>
                <div class="fs-4 fw-bold text-success">
                  <%= agencies.filter(a => a.sales_count > 0).length %>
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="card bg-light">
              <div class="card-body text-center">
                <h6 class="card-title">総売上件数</h6>
                <div class="fs-4 fw-bold text-info">
                  <%= agencies.reduce((sum, a) => sum + (a.sales_count || 0), 0)
                  %>件
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
