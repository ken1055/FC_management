<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body class="bg-light">
    <div class="container py-4">
      <h1 class="h4 mb-3"><i class="bi bi-eye"></i> 取引詳細</h1>
      <div class="card">
        <div class="card-body">
          <div class="row g-3">
            <div class="col-md-4">
              <label class="form-label">取引ID</label>
              <div class="form-control-plaintext"><%= transaction.id %></div>
            </div>
            <div class="col-md-4">
              <label class="form-label">取引日</label>
              <div class="form-control-plaintext"><%= new Date(transaction.transaction_date).toLocaleDateString('ja-JP') %></div>
            </div>
            <div class="col-md-4">
              <label class="form-label">登録日時</label>
              <div class="form-control-plaintext"><%= new Date(transaction.created_at).toLocaleString('ja-JP') %></div>
            </div>
            <div class="col-md-6">
              <label class="form-label">金額</label>
              <div class="form-control-plaintext text-success fw-bold">¥<%= transaction.amount.toLocaleString() %></div>
            </div>
            <div class="col-md-6">
              <label class="form-label">支払方法</label>
              <div class="form-control-plaintext"><%= transaction.payment_method || '現金' %></div>
            </div>
            <div class="col-12">
              <label class="form-label">取引内容</label>
              <div class="form-control-plaintext"><%= transaction.description || '-' %></div>
            </div>
            <div class="col-md-6">
              <label class="form-label">店舗</label>
              <div class="form-control-plaintext"><%= storeName || '-' %></div>
            </div>
            <div class="col-md-6">
              <label class="form-label">顧客</label>
              <div class="form-control-plaintext">
                <% if (customer) { %>
                  <%= customer.name %>
                  <% if (customer.customer_code) { %>
                    <small class="text-muted">(<%= customer.customer_code %>)</small>
                  <% } %>
                <% } else { %>
                  -
                <% } %>
              </div>
            </div>
          </div>
          <div class="mt-4">
            <a href="/sales/history" class="btn btn-outline-secondary">戻る</a>
          </div>
        </div>
      </div>
    </div>
  </body>
  </html>


