<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <style>
      .page-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }
      .card-section-title {
        font-weight: 600;
        font-size: 0.95rem;
        color: #6c757d;
      }
      .value-lg {
        font-size: 1.25rem;
        font-weight: 800;
      }
      .muted {
        color: #6c757d;
      }
    </style>
  </head>
  <body class="bg-light">
    <div class="container py-4">
      <div class="d-flex justify-content-between align-items-start mb-3">
        <div class="page-header">
          <span class="badge bg-secondary rounded-pill"
            ><i class="bi bi-hash"></i> <%= transaction.id %></span
          >
          <h1 class="h4 m-0">
            <i class="bi bi-receipt-cutoff text-primary"></i> 取引詳細
          </h1>
        </div>
        <a href="/sales/history" class="btn btn-outline-secondary btn-sm"
          ><i class="bi bi-arrow-left"></i> 履歴に戻る</a
        >
      </div>

      <div class="row g-3">
        <div class="col-lg-7">
          <div class="card shadow-sm">
            <div class="card-body">
              <div class="row g-3 align-items-center">
                <div class="col-6">
                  <div class="card-section-title">金額</div>
                  <div class="value-lg text-success">
                    ¥<%= transaction.amount.toLocaleString() %>
                  </div>
                </div>
                <div class="col-6 text-lg-end">
                  <span class="muted"
                    ><i class="bi bi-calendar-event"></i> 取引日</span
                  >
                  <div>
                    <%= new
                    Date(transaction.transaction_date).toLocaleDateString('ja-JP')
                    %>
                  </div>
                </div>
                <div class="col-6">
                  <div class="card-section-title">支払方法</div>
                  <span class="badge bg-secondary"
                    ><i class="bi bi-wallet2"></i> <%=
                    transaction.payment_method || '現金' %></span
                  >
                </div>
                <div class="col-6 text-lg-end">
                  <span class="muted"
                    ><i class="bi bi-clock-history"></i> 登録</span
                  >
                  <div>
                    <%= new Date(transaction.created_at).toLocaleString('ja-JP')
                    %>
                  </div>
                </div>
              </div>
              <hr />
              <div class="card-section-title mb-1">取引内容</div>
              <div class="p-2 bg-light rounded">
                <%= transaction.description || '-' %>
              </div>
            </div>
          </div>
        </div>
        <div class="col-lg-5">
          <div class="card shadow-sm mb-3">
            <div class="card-body">
              <div class="card-section-title mb-2">
                <i class="bi bi-building"></i> 店舗
              </div>
              <div class="ps-1"><%= storeName || '-' %></div>
            </div>
          </div>
          <div class="card shadow-sm">
            <div class="card-body">
              <div class="card-section-title mb-2">
                <i class="bi bi-person"></i> 顧客
              </div>
              <% if (customer) { %>
              <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex justify-content-between">
                  <span class="muted">氏名</span>
                  <span><%= customer.name %></span>
                </li>
                <% if (customer.customer_code) { %>
                <li class="list-group-item d-flex justify-content-between">
                  <span class="muted">顧客コード</span>
                  <span><%= customer.customer_code %></span>
                </li>
                <% } %>
              </ul>
              <% } else { %>
              <div class="ps-1">-</div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
