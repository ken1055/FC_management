<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body class="bg-light">
    <div class="container py-4">
      <h1 class="h4 mb-3"><i class="bi bi-gear"></i> 売上フィルター設定</h1>
      <div class="card">
        <div class="card-body">
          <form method="GET" action="/sales/history">
            <div class="row g-3">
              <% if (isAdmin) { %>
              <div class="col-md-4">
                <label class="form-label">店舗</label>
                <select name="store_id" class="form-select">
                  <option value="all">全店舗</option>
                  <% stores.forEach(s => { %>
                    <option value="<%= s.id %>" <%= defaults.store_id == s.id ? 'selected' : '' %>><%= s.name %></option>
                  <% }) %>
                </select>
              </div>
              <% } %>
              <div class="col-md-4">
                <label class="form-label">開始日</label>
                <input type="date" class="form-control" name="start_date" value="<%= defaults.start_date %>" />
              </div>
              <div class="col-md-4">
                <label class="form-label">終了日</label>
                <input type="date" class="form-control" name="end_date" value="<%= defaults.end_date %>" />
              </div>
              <div class="col-12">
                <label class="form-label">顧客検索</label>
                <input type="text" class="form-control" name="customer_search" value="<%= defaults.customer_search %>" placeholder="顧客名・コード" />
              </div>
            </div>
            <div class="mt-3">
              <button class="btn btn-primary" type="submit"><i class="bi bi-search"></i> この条件で表示</button>
              <a href="/sales/history" class="btn btn-outline-secondary ms-2">クリア</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
  </html>


