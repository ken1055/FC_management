<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>グループ管理 - 店舗管理システム</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"
    />
  </head>
  <body class="container py-5">
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4 rounded">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">店舗管理システム</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if (typeof session !== 'undefined' && session && session.user &&
            session.user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/stores/list">店舗一覧</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/sales/list">売上管理</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/groups/list">グループ管理</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/users/list">アカウント管理</a>
            </li>
            <% } %> <% if (typeof session !== 'undefined' && session &&
            session.user && session.user.role === 'agency') { %>
            <li class="nav-item">
              <a class="nav-link text-warning" href="/auth/promote"
                >管理者・役員に切り替え</a
              >
            </li>
            <% if (session.user.store_id) { %>
            <li class="nav-item">
              <a
                class="nav-link"
                href="/stores/profile/<%= session.user.store_id %>"
                >マイプロフィール</a
              >
            </li>
            <% } %>
            <li class="nav-item">
              <a class="nav-link" href="/sales/list">売上管理</a>
            </li>
            <% if (session.user.store_id) { %>
            <li class="nav-item">
              <a
                class="nav-link"
                href="/materials/<%= session.user.store_id %>"
                >商品資料格納庫</a
              >
            </li>
            <% } %> <% } %> <% if (typeof session !== 'undefined' && session &&
            session.user && (session.user.role === 'admin')) { %> <% } %> <% if
            (typeof session !== 'undefined' && session && session.user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/auth/logout">ログアウト</a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/auth/login">ログイン</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="card shadow-sm p-4 mb-4">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">グループ管理</h2>
        <div>
          <a href="/groups/new" class="btn btn-success me-2">
            <i class="bi bi-plus-circle"></i> 新規グループ作成
          </a>
          <a href="/" class="btn btn-secondary">ダッシュボードへ戻る</a>
        </div>
      </div>

      <div class="mb-3">
        <p class="text-muted">
          店舗をグループ別に管理できます。グループを作成して店舗を割り当て、効率的な管理を行ってください。
        </p>
      </div>

      <!-- グループ一覧 -->
      <% if (groups.length === 0) { %>
      <div class="text-center py-5">
        <i class="bi bi-collection fs-1 text-muted"></i>
        <h5 class="mt-3 text-muted">グループがありません</h5>
        <p class="text-muted">最初のグループを作成してください。</p>
        <a href="/groups/new" class="btn btn-primary">
          <i class="bi bi-plus-circle"></i> グループを作成
        </a>
      </div>
      <% } else { %>

      <div class="row g-3">
        <% groups.forEach(group => { %>
        <div class="col-md-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div
                class="d-flex justify-content-between align-items-start mb-3"
              >
                <h5 class="card-title mb-0">
                  <i class="bi bi-collection text-primary"></i>
                  <%= group.name %>
                </h5>
                <span class="badge bg-info"><%= group.agency_count %>社</span>
              </div>

              <div class="mb-3">
                <div class="text-center">
                  <div class="fs-6 text-muted">所属店舗数</div>
                  <div class="fs-4 fw-bold text-primary">
                    <%= group.agency_count %>社
                  </div>
                </div>
              </div>

              <div class="d-grid gap-2">
                <a
                  href="/groups/manage/<%= group.id %>"
                  class="btn btn-primary"
                >
                  <i class="bi bi-plus-circle"></i> 店舗を追加
                </a>
                <div class="btn-group" role="group">
                  <a
                    href="/groups/edit/<%= group.id %>"
                    class="btn btn-outline-secondary btn-sm"
                  >
                    <i class="bi bi-pencil"></i> 編集
                  </a>
                  <form
                    method="post"
                    action="/groups/delete/<%= group.id %>"
                    style="display: inline"
                    onsubmit="return confirm('「<%= group.name %>」を削除しますか？所属する店舗の割り当ても解除されます。')"
                  >
                    <button type="submit" class="btn btn-outline-danger btn-sm">
                      <i class="bi bi-trash"></i> 削除
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>

      <!-- 統計情報 -->
      <div class="mt-4">
        <div class="row">
          <div class="col-md-6">
            <div class="card bg-light">
              <div class="card-body text-center">
                <h6 class="card-title">総グループ数</h6>
                <div class="fs-4 fw-bold text-primary">
                  <%= groups.length %>
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-6">
            <div class="card bg-light">
              <div class="card-body text-center">
                <h6 class="card-title">総所属店舗数</h6>
                <div class="fs-4 fw-bold text-success">
                  <%= groups.reduce((sum, g) => sum + (g.agency_count || 0), 0)
                  %>社
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
